Snake game made simple with speech recognition for the controls :) .

 
###how to install and run 

clone the repo 
Run `npm install` to install the dependencies , ( as speech recognition requires secure host i.e, `https` or `localhost` ) 
Run `npm start`
Try the game :) 

